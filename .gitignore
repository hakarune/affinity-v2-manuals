# Build output
/dist/
/.work/
/book/
/site/

# Image-backfill scratch space kept locally, not committed
sources/**/Missing images/
sources/**/missing images/

# Re-packed archives (regenerated by scripts/build.sh)
*.zip

# Obsidian per-machine state (the vault config in config/obsidian/ IS tracked)
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/workspace.json.bak

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
