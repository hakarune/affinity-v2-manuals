# ![Contour Tool](../../../assets/shared/ui/Contour_tool.png)

 Contour Tool

The **Contour Tool** lets you offset the stroke of a curve or shape.

The tool offsets the object's original outline; the stroke moves with the newly offset outline but its width remains unchanged. The object's outline can be moved inwardly or outwardly (more typically) by dragging.

> **Tip:** Tool shortcut : `O`.

### Settings

The following settings can be adjusted from the context toolbar:

- **Fill**—click the colour swatch to display a pop-up panel to update fill colour.
- **Stroke**—click the colour swatch to display a pop-up panel to update stroke colour.
- **Stroke properties**—the line thickness in points. Type directly in the text box or drag the pop-up slider to set the value.
- **Radius**—sets the radius of the contour, i.e. the distance that the object's outline will be offset.
- **Contour type**—Select a contour type (Round, Bevel or Mitre) to determine the contour of sharp corners. With **Mitre** joins, the **Mitre** limit setting dynamically controls if bevelling occurs by extending each line at the junction by the number or radius multiples. If the two outer edges meet within that limit, the result is a sharp corner; if not, you’ll get a flat (Bevel) corner.
- **Mitre**—sets the limit to which the extension of mitre joins will create either sharp or flat corners. Contour Type must be set to Mitre.
- **Cap**—set the cap ends to **No cap**, **Round cap** or **Square cap**. Use on an already contoured curve's end nodes.
- **Fill**—set the shape's fill to **Auto closed**, **Force open** or **Force closed**.
- **Bake Appearance**—converts the contoured object to curves. The created contour will no longer be editable if this option is selected.

#### SEE ALSO:

- [Contouring curves and shapes](../../05-drawing-curves-and-shapes/11-contouring-curves-and-shapes.md)
- [Edit curves and shapes](../../05-drawing-curves-and-shapes/03-edit-curves-and-shapes.md)
- [Draw and edit shapes](../../05-drawing-curves-and-shapes/06-draw-and-edit-shapes.md)
- [Keyboard shortcuts for tools](../../26-keyboard-shortcuts/01-keyboard-shortcuts.md)
