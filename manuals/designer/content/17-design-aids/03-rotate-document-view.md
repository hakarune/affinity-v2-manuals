# Rotate document view

Rather than rotating the document permanently, you can rotate the document view instead. This allows you to view your document from a different angle without manipulating its layout.

![Rotated document view](../../assets/shared/rotate_document_view.png)

## About document view rotation

Being able to rotate the document view is beneficial for a number of applications:

- Brush and fine detail work
- Product design (e.g., product box/packaging layouts with content at 90/180 degree angles)
- Tablet-assisted design (drawing at angles)

**To rotate the document view:**

- On the **View** menu, select **Rotate Left** or **Rotate Right**. The document view will be rotated in 15° increments.

**To reset the document view back to its original rotation:**

Do one of the following:

- On the **View** menu, select **Reset Rotation**.
- Press `Ctrl`+`Shift`+`Cmd`+`R`.
- Press `Cmd`+`Alt`+`Shift`+`R`.

> **Tip:** For greater efficiency, assign your own custom keyboard shortcuts to rotating and resetting document view operations. See [Customising keyboard shortcuts](../21-workspace/customise/01-keyboard-shortcuts.md) for more information.

**macOS:**

**To switch on document view rotation using a scroll wheel:**

- From **Affinity Designer>Settings** (or **>Preferences**) (Tools option), check **Enable canvas rotation with `Cmd`+scroll wheel**.

**Windows:**

**To switch on document view rotation using a scroll wheel:**

- From **Edit>Settings** (Tools option), check **Enable canvas rotation with `Alt`+scroll wheel**.

> **Note:** ### Modifier keys
>
>
> With the above Preference set, the following modifier keys can be used to modify rotation behaviour using a mouse scroll wheel:
>
>
> - Hold down the `Cmd`  while scrolling to rotate around the mouse cursor location.
> - Hold down the `Alt`  while scrolling to rotate around the mouse cursor location.
> - To rotate about the document view centre, hold down the `Shift`  and the `Alt`  while scrolling.

#### SEE ALSO:

- [Using a trackpad](../25-extras/third-party-support/02-using-a-trackpad.md)
- [Settings (or Preferences)](../27-settings-preferences/01-settings-preferences.md)
- [Keyboard shortcuts for document view](../26-keyboard-shortcuts/01-keyboard-shortcuts.md)
