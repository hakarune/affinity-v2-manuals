# Layers panel (Export Persona)

The Layers panel in Export Persona lets you create slices from entire layers, groups or selected objects.

## About the Layers panel

The panel lets you target layers, groups or objects in your document that you want to specifically export. By selecting them, they can be made into slices from the same panel; the Create Slice option generates slices for the Slices panel from which raster images can be exported.

![Layers panel (Export Persona)](../../assets/images/panel_layers_exportpersona.png)
*The Layers panel showing layers in the current document.*

### Options

The following options are available in the panel:

- ![Layer is exported](../../assets/shared/ui/exported.png) **Layer is exported**—indicates the layer export state.
- ![Layer has exported children](../../assets/shared/ui/exportedchildren.png) **Layer has exported children**—indicates that the exported layer contains objects in child layers.
- ![Make item visible in export](../../assets/shared/ui/export.png) **Make item visible in export**—when enabled, the item is visible and can be exported. When disabled, the item is hidden and can't be exported.
- ![Make item visible](../../assets/shared/ui/export-on_copy.png) **Make item visible**—when enabled, the item is visible. When disabled, the item is hidden. The item can be exported in both states.
- ![Edit All Layers](../../assets/shared/ui/edit_all_layers.png) **Edit All Layers**—allows selection of objects across all layers (rather than the current layer).
- **Create Slice**—creates a slice from the selected artboard, layer, group or object in readiness for export.

> **Tip:** For child objects, the visibility options may show as being partly grayed out if the parent object has the corresponding visibility option switched off.

#### SEE ALSO:

- [Exporting using Export Persona](01-exporting-using-export-persona.md)
- [Slices panel](03-slices-panel.md)
- [Layers panel](../33-panels/13-layers-panel.md)
- [Customizing the workspace](../31-workspace/04-customize/02-workspace.md)
