# Zooming

Zooming into areas of your document is an essential part of the design process. Several zooming options are possible.

You can use the **Navigator** panel to save the document view's current zoom level and position. Multiple view points can be saved, and you can instantly navigate between them.

**To zoom in or out of a document:**

Do one of the following:

- Click the **Zoom Tool** (shortcut `Z`) and then:
  - Drag on the page—right to zoom in or left to zoom out.*
  - Click on the page to zoom in a step.
  - `Alt`-click on the page to zoom out a step.
  - `Alt`-drag on the page to zoom into an area defined by a marquee.
  - Select a zoom level from the context toolbar.
- Click the **View Tool** (shortcut `H`) and select a zoom level from the context toolbar.
- Select the **View** menu and then an option from the `Zoom` pop-up.
- Use `Alt` and your mouse scroll wheel (if available).
- Zoom in or out using the options in the **Navigator** panel.

> **Note:** *This is the default behavior. You can choose to switch this to zoom into an area defined by a marquee from [Settings (Tools options)](../25-settings-preferences/01-settings-preferences.md).

> **Tip:** Shortcuts:
>
> - **Zoom In** using `Cmd` + `+`.
> - **Zoom Out** using `Cmd` + `-`.
> - **Zoom to Fit** using `Cmd`0 or double-click the View Tool icon or Page panel's page numbers.
> - **Zoom to 100%** using `Cmd`1 or double-click the Zoom Tool icon, where document pixels map directly to screen pixels.
> - **macOS:** **Zoom to Selection** using `Shift` + `Cmd` + `0` (or double-click Layers panel thumbnail).
> - **Windows:** **Zoom to Selection** using `Cmd` + `Alt` + `0` (or double-click Layers panel thumbnail.
> - **Actual Size** using `Cmd`8.
> - **Pixel Size** using `Cmd`9.

> **Note:** When creating a new document, the Document Units define the exact size of your document on screen. For example, if you chose 'pixels', your document will display at its exact pixel size when exported and viewed on screen at 100%. For physical Document Units (e.g., millimeters) you'll get the exact print size at 50% zoom (due to Retina screen). The print size is determined by the dpi set in **Document Setup**.

> **Preferences — Settings (or Preferences):** Related behaviors can be adjusted from [the app's settings](../25-settings-preferences/01-settings-preferences.md):
>
> - **General>Limit initial zoom to a maximum of 100%**

#### SEE ALSO:

- [Navigator panel](../21-panels/15-navigator-panel.md)
- [Zoom Tool](../20-tools/01-layout-tools/15-zoom-tool.md)
- [View](08-viewing.md)
- **macOS:** [Using a trackpad](../23-extras/04-third-party-support/02-using-a-trackpad.md)
- [Keyboard shortcuts for the document view](../24-keyboard-shortcuts/01-keyboard-shortcuts.md)
