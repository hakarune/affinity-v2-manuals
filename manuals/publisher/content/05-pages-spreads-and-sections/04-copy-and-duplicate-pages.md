# Copy and duplicate pages

Additional copies of pages can be created wherever they're needed within a document.

There are two methods for achieving this:

- Duplicate pages immediately after the last page in your selection.
- Copy the selected pages to your chosen position, before or after a specific page number.

The selected pages need not be a continuous range.

The order of the new pages will match the originals.

## Copying pages using drag and drop

**macOS:** Pages can be copied by holding the `Alt`  while dragging and dropping their thumbnails.

**Windows:** Pages can be copied by holding the `Cmd`  while dragging and dropping their thumbnails.

When dragging page thumbnails:

- A blue insertion point indicates the where the pages will be positioned if dropped.
- The cursor indicates whether pages will be inserted as new spreads or extend the targeted spread.

Available positions vary according to whether **Reflow Pages** (on the Pages panel's preferences menu) is enabled or disabled.

When Reflow Pages is enabled, pages will reflow starting at whichever is earliest—either the first page in the selection or the position where you drop—through to the end of your document.

When Reflow Pages is disabled, pages will not automatically reflow. This may result in 'ghost' pages.

**To duplicate pages:**

1. On the **Pages** panel:
  1. Select one or more publication pages or a master page.
  2. Select **Duplicate Selected Pages** or **Duplicate Selected Master** as appropriate. The new pages are created immediately after your selection.

> **Note:**
>
> **macOS:** Alternatively, for publication pages, -drag the selected pages and drop where you want to duplicate them.
>
> **Windows:** Alternatively, for publication pages, -drag the selected pages and drop where you want to duplicate them.

**To copy pages (using drag and drop):**

On the **Pages** panel:

1. Drag one or more publication pages or a master page.
2. Hover where you want to copy the pages and drop when the cursor indicates the required action will be performed.

> **Note:** When copying master pages, be careful not to drop one master onto another, which will apply the former to the latter.

**To copy publication pages (via dialog):**

1. On the **Pages** panel:
  1. Select one or more publication pages<sup>1</sup>.
  2. `Click`-click one of the selected pages and select **Move/Copy Pages**.
2. On the dialog that appears:
  1. Specify the destination **Page** number for the new pages, and whether to position the new pages **Before** or **After** it.
  2. Select **Copy**.
  3. Select **OK**.

<sup>1</sup> Alternatively, you can specify the pages later, by typing their numbers on the dialog.

#### SEE ALSO:

- [Add and remove pages](02-add-and-remove-pages.md)
- [Arrange pages](03-arrange-pages.md)
- [About master pages](12-master-pages/01-about-master-pages.md)
- [Pages panel](../21-panels/17-pages-panel.md)
- [About ghost pages](05-about-ghost-pages.md)
- [Keyboard shortcuts for page control](../24-keyboard-shortcuts/01-keyboard-shortcuts.md)
